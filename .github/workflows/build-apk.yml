name: Build Android APK (desativado)
  # Este workflow foi desativado — use o eas-build.yml
  on:
    workflow_dispatch:
      inputs:
        disabled:
          description: 'Use eas-build.yml instead'
          default: 'true'

  jobs:
    noop:
      runs-on: ubuntu-latest
      steps:
        - run: echo "Use eas-build.yml for building APKs"
  